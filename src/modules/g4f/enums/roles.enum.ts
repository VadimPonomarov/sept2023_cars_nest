export enum GptRolesEnum {
  SYSTEM = 'system', // Used for providing instructions and context prior to the conversation.
  USER = 'user', // Used to identify user messages
  ASSISTANT = 'assistant', // Used to identify AI messages
}
